{
  "name": "sealskin",
  "version": "0.3.0",
  "description": "SealSkin mobile shell (Capacitor). The web dir is copied from client/dist/mobile.",
  "main": "index.html",
  "scripts": {
    "build": "rm -rf www && (cd ../client && ([ -d node_modules ] || npm install --no-audit --no-fund) && SEALSKIN_BUILD_STRICT=1 npm run build --silent -- --target mobile) && cp -r ../client/dist/mobile www && npx @capacitor/assets generate --iconBackgroundColor '#12161d' --splashBackgroundColor '#12161d'",
    "android": "npm run build && npx cap sync android && npx cap open android",
    "ios": "npm run build && npx cap sync ios && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.1",
    "@capacitor/app": "^8.1.1",
    "@capacitor/browser": "^8.0.4",
    "@capacitor/core": "^8.5.1",
    "@capacitor/filesystem": "^8.1.3",
    "@capacitor/ios": "^8.5.1",
    "@capawesome-team/capacitor-file-opener": "^8.0.3",
    "capacitor-blob-writer": "^1.0.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.0",
    "@capacitor/cli": "^8.5.1"
  }
}
